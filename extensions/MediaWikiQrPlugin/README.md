# MediaWikiQrPlugin

A MediaWiki extension that adds a QR code to every wiki page. The QR code
encodes the canonical URL of the page it appears on, so you can print a page
(or just the QR code) for offline use and still find your way back to the
source document by scanning the code with any phone or tablet.

Clicking on the QR brings up a page with just the QR and URL. This can be printed and attached to a shop machine so that you can find more information such as runbooks, maintenance manuals, and so on.

## Features

- Renders a QR code after the content of every page, in every namespace
  (articles, Talk, Special, etc.).
- Encodes the page's full canonical URL (scheme, host, port, and path), so the
  code resolves to exactly the page it was printed from.
- Generates the QR code entirely server-side and embeds it as an inline SVG
  image. No third-party QR service or URL shortener is contacted, and no data
  about your pages leaves your server.
- Ships with its 3rd-party QR library bundled, so it installs without running
  Composer.

## Requirements

- MediaWiki 1.43.0 or newer
- PHP 8.2 or newer
- [Composer](https://getcomposer.org/) to download and install the PHP
  dependencies

## Installation from source

1. Clone (or copy) this repository into your MediaWiki `extensions/` directory
   as `MediaWikiQrPlugin`:

   ```bash
   cd extensions
   git clone <repository-url> MediaWikiQrPlugin
   ```

2. Install the PHP dependencies. This populates `vendor/`, which is **not**
   committed to the repository:

   ```bash
   cd MediaWikiQrPlugin
   composer install --no-dev
   ```

3. Load the extension by adding the following line to your `LocalSettings.php`:

   ```php
   wfLoadExtension( 'MediaWikiQrPlugin' );
   ```

4. Reload any wiki page. A QR code linking to that page now appears below the
   page content.

## Generating an Archive for Distribution

```bash
make dist
```

## Project layout

```
MediaWikiQrPlugin/
├── extension.json   # Extension manifest (name, hooks, autoloading, requirements)
├── composer.json    # PHP dependency definition
├── composer.lock    # Pinned dependency versions for reproducible installs
├── src/
│   └── Hooks.php     # SkinAfterContent hook that renders the QR code
├── LICENSE
└── README.md
```

## Bundled Third Party Libraries

The QR code itself is generated by the
[chillerlan/php-qrcode](https://github.com/chillerlan/php-qrcode) library,
pulled in via Composer.

## Contributing

Contributions are welcome. To get started:

1. Fork the repository and create a feature branch.
2. Run `composer install` to set up the dependencies.
3. Make your change in `src/`, keeping the existing code style.
4. Open a pull request describing the change and the motivation behind it.

Please keep pull requests focused and include a clear description of what the
change does and why.

## License

Released under the MIT License. See [LICENSE](LICENSE) for the full text.
