# Runtime cache (regenerated by MediaWiki)
/cache/*

# Uploads live on Cloudflare R2; keep the tarball's stubs only
/images/*
!/images/.htaccess
!/images/README

# Machine-local secrets live at ../secrets.php (outside the webroot)
